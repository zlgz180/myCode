1.工厂容器初始化
抽象工厂里的refresh方法
加锁
初始化时间戳和状态
初始化bean工厂
加载配置文件，初始化bean定义，注册baen定义
初始化特殊的bean
初始化bean工厂前后置处理器
注册广播事件
实例化对象
广播事件


BeanFactory和FactoryBean
BeanFactory是bean的工厂
factoryBean是一个工厂的抽象，返回的是生产的对象，不是工厂本身



2.实例化对象（先实例化，后初始化，然后前后置处理器）
检查依赖对象，递归实例化
检查是否有循环引用
检查缓存是否重复实例化
实例化（jdk反射(接口)，cglib（有覆写方法））
依赖注入属性和引用（构造器，set，initMethon）
beanPostProcesscer前置处理器
调用初始化方法
beanPostProcesscer后置处理器


3.如何解决循环依赖
单例对象使用全局对象缓存判重
多例对象使用局部缓存

4.依赖注入的时机
第一次getBean的时候

